void setup() {
  Serial.begin(115200);
}

void loop() {
  if (Serial.available() > 0) {
    String text = Serial.readStringUntil('\n');
    text.trim();
    Serial.print("Rebut: ");
    Serial.println(text);
  }
}
